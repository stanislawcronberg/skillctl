# Rust build artifacts (Cargo.lock is intentionally tracked — this is a binary)
/target

# macOS finder droppings
.DS_Store

# Local, machine-specific Claude Code permissions (settings.json is shared)
.claude/settings.local.json

# Local throwaway test fixture: it carries its own .git history and is
# regenerated by hand, so it is not tracked here.
/test-marketplace
