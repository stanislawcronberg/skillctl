# Rust build artifacts
/target

# Local, machine-specific Claude Code permissions (settings.json is shared)
.claude/settings.local.json

# Throwaway empirical-verification fixture; it carries its own .git history
# and is regenerated by hand, so it is not tracked here.
/test-marketplace
